document.addEventListener("DOMContentLoaded", () => {
  // // 텍스트 동적으로 (문구수정가능)
  // const dynamicText = document.getElementById("dynamic-text");
  // const originalText = "지구를 위한 친환경사업이 고객에게 큰 혜택으로 돌아 갈 수 있도록 섬세하고 세밀하게 분석하여 방안을 도출하는 고객중심 마인드로 비즈니스에 임하고 있습니다. 줌인에너지 고객의 혜택이 글로벌환경 문제의 해결방안이 되도록 최선을 다하겠습니다.";
  // let currentIndex = 0;

  // function appendNextCharacter() {
  // if (currentIndex < originalText.length) {
  // dynamicText.textContent += originalText[currentIndex];
  // currentIndex++;

  // setTimeout(appendNextCharacter, 30); // 한 글자씩 나오는 속도를 조절할 수 있습니다.
  // }
  // }

  // appendNextCharacter();

  AOS.init();
});
